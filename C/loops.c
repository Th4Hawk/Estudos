#include <stdio.h>

int main() {
    // Loop for
    for (int num = 0; num < 20; num++) {
        printf("%d\n", num);
    }

    printf("-------------------------------------------------");
    
    // Loop while
    int num = 0;
    while (num < 20) {
        printf("%d\n", num);
        num++;
    }
}